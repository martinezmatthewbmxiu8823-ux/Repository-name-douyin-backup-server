# 🚀 一键部署指南

老子已经为你准备好了所有文件！现在只需3步就能部署到免费服务器！

---

## 📋 方案选择

### 🥇 推荐：Railway（最简单）
- 每月$5免费额度
- 自动检测Dockerfile
- 内置MongoDB
- 3分钟部署完成

### 🥈 备选：Render + MongoDB Atlas
- 完全免费
- 需要单独配置数据库
- 15分钟无请求会休眠

---

## 🎯 Railway一键部署（推荐）

### 1. 访问Railway并登录
```
https://railway.app/
用GitHub账号登录
```

### 2. 创建新项目
```
点击 "New Project"
选择 "Deploy from GitHub repo"
连接你的GitHub账号
```

### 3. 推送代码到GitHub

在PowerShell执行：

```powershell
cd C:\Users\Administrator\Desktop\精品\竞品助手_服务器版

# 初始化git
git init
git add .
git commit -m "竞品助手备份服务器"

# 在GitHub创建仓库：https://github.com/new
# 仓库名：douyin-backup-server
# 设为Public

# 推送
git remote add origin https://github.com/你的用户名/douyin-backup-server.git
git branch -M main
git push -u origin main
```

### 4. 在Railway部署

```
1. 选择刚创建的douyin-backup-server仓库
2. Railway自动检测到Dockerfile并开始部署
3. 点击项目 → "New" → "Database" → "Add MongoDB"
4. 添加环境变量：
   - 点击服务 → "Variables"
   - 添加 DOUYIN_COOKIES: [你的Cookie]
5. 等待部署完成（2-3分钟）
```

### 5. 获取服务器地址

```
点击服务 → "Settings" → "Generate Domain"
你的服务器：https://xxx.railway.app
```

### 6. 测试

访问：`https://xxx.railway.app/api/stats`

---

## 🔧 提取Cookie（重要！）

在罗盘页面Console执行：

```javascript
copy(JSON.stringify(
  document.cookie.split(';').map(c => {
    const [name, ...v] = c.trim().split('=');
    return {
      name: name,
      value: v.join('='),
      domain: '.jinritemai.com'
    };
  })
));
```

Cookie已复制，粘贴到Railway环境变量中。

---

## ✅ 部署完成后

### 手动触发备份

```bash
curl -X POST https://你的域名.railway.app/api/backup/trigger
```

### 查看统计

```bash
curl https://你的域名.railway.app/api/stats
```

### 查询历史数据

```bash
# 查看今天的数据
curl https://你的域名.railway.app/api/video/2026-07-04

# 查看日期范围
curl https://你的域名.railway.app/api/video?startDate=2026-01-01&endDate=2026-07-04
```

---

## 📱 在Chrome扩展中使用

把服务器地址写入扩展：

```javascript
const SERVER_URL = 'https://你的域名.railway.app';

// 查询历史数据
fetch(`${SERVER_URL}/api/video/2026-01-01`)
  .then(r => r.json())
  .then(data => {
    console.log('历史数据:', data);
  });
```

---

## 🎉 最终效果

- ✅ 服务器24小时运行
- ✅ 每天凌晨2点自动备份
- ✅ 数据永久保存在MongoDB
- ✅ 通过API随时查询
- ✅ 360天后拥有完整历史数据

---

## ⚠️ 注意事项

1. **Cookie会过期**：每隔一段时间需要更新
2. **免费额度**：Railway每月$5，大约500小时运行时间
3. **备份时间**：在server.js中修改cron表达式调整
4. **数据导出**：定期导出MongoDB数据备份

---

## 🆘 遇到问题？

### 问题1：部署失败
```bash
# 查看Railway日志
在Railway控制台 → "Deployments" → 点击最新部署 → "View Logs"
```

### 问题2：备份失败
```bash
# 检查Cookie是否有效
# 在服务器日志中查看错误信息
# 更新Cookie环境变量
```

### 问题3：数据库连接失败
```bash
# 确认MongoDB服务已启动
# 检查MONGO_URL环境变量
# 重启服务
```

---

**立即开始部署！360天后你就是数据大师！** 🚀🔥

---

作者：海鸥
版本：v1.0
日期：2026-07-04
